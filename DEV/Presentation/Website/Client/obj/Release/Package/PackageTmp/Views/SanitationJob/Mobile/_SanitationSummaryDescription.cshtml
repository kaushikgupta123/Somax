@model Client.Models.Sanitation.SanitationVM
<div class="slide-modal" id="sanitationdetaildesmodal">
    <div class="slide-modal-head">
        <a class="slide-back-btn" id="sanitationdetaildesmodalHide"> <i class="fa fa-angle-left"></i> Back</a>
        <div class="slide-back-title summarydescriptionmodaltitle">
            @Model.Loc.Where(a => a.ResourceId == "spnDescription").FirstOrDefault().Value.ToString()
        </div>
    </div>
    <div class="slide-modal-body" style="text-align:center;">
        <span id="sanitationdetaildesmodaltext"></span>
    </div>
</div>


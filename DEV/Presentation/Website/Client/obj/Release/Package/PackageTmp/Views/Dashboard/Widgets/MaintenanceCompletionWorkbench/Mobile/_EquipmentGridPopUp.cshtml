@using Client.Models.Dashboard
@model ScrollViewModel<EquipmentScrollViewModel>



@*<div class="demo-paging">
        <div style="height:100%">
            <div class="md-paging" id="equipment-md-paging">
                @if (Model.LookupList.Count() > 0)
                {
                    <ul class="md-category mbsc-cloak" id="Equipment-md-category">
                        @for (var i = 0; i < Model.PageNos.Count; i++)
                        {
                            var item = Model.PageNos[i];
                            if (i == 0)
                            {
                                <li data-id="@item" data-page="md-@item" data-selected="true">@item</li>
                            }
                            else
                            {
                                <li data-id="@item" data-page="md-@item">@item</li>
                            }

                        }
                    </ul>

                    <ul class="md-content mbsc-cloak" id="Equipment-md-content">
                        @for (var i = 0; i < Model.PageNos.Count; i++)
                        {
                            var Pageno = Model.PageNos[i];
                            var data = Model.LookupList.Where(x => x.Pageno == Pageno).ToList();

                            <li class="md-@Pageno">
                                <ul class="md-music-lv md-list mbsc-cloak Equipment-md-list">
                                    @foreach (var item in data)
                                    {
                                        <li class="scrollview-equipmentsearch" data-id="@item.EquipmentId" data-clientlookupid="@item.ClientLookupId">
                                            <h4>@item.ClientLookupId</h4>
                                            <p>@item.Name</p>
                                        </li>
                                    }
                                </ul>
                            </li>
                        }
                    </ul>
                }
                else
                {
                    <ul class="md-content mbsc-cloak" id="Equipment-md-content">
                        <li class="text-center">
                            No data available in the table
                        </li>
                    </ul>
                }
            </div>
        </div>
    </div>*@

<ul id="AssetListViewForSearch" class="mbsc-cloak" mbsc-enhance>
    @*<li data-role="list-divider">Top Rated Movies</li>*@
</ul>
<div class="mbsc-padding" mbsc-form  style="margin-bottom:80px;">
    <button mbsc-button id="btnAssetLoadMore" class="btn btn-blue mobBttn" data-color="success">Load More</button>
</div>
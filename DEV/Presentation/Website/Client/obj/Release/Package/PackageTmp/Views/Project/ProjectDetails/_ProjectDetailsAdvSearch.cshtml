@model Client.Models.Project.ProjectVM
<form>
    <nav id="sidebar">
        <div id="dismiss">
            <i class="fa fa-close"></i>
        </div>
        <div class="sidebar-header">
            <h3><i class="fa fa-filter" style="position: relative; top: -2px;"></i> @Model.Loc.Where(a => a.ResourceId == "globalFilter").FirstOrDefault().Value.ToString()<span class="filteritemcountPTL">0</span></h3>
            @*<button type="button" class="btn btn-blue filterSearch" id="btnLSDataAdvSrch"><i class="fa fa-search" style="margin-top:-3px;"></i> &nbsp; @Model.Loc.Where(a => a.ResourceId == "globalSearch").FirstOrDefault().Value.ToString()</button>*@
            <button type="button" class="btn btn-blue filterSearch" id="btnPTLDataAdvSrch"> @Model.Loc.Where(a => a.ResourceId == "GlobalApply").FirstOrDefault().Value.ToString()</button>
        </div>
        <div class="sidebar-content" id="advsearchsidebar">
            <div class="form-group">
                <label for="WorkOrderClientLookupId">@Model.Loc.Where(a => a.ResourceId == "spnGlobalWorkOrderID").FirstOrDefault().Value.ToString()  :</label>
                <input type="text" class="form-control search adv-item" id="WorkOrderClientLookupId" autocomplete="off">
            </div>
            <div class="form-group">
                <label for="Description">@Model.Loc.Where(a => a.ResourceId == "spnDescription").FirstOrDefault().Value.ToString() :</label>
                <input type="text" class="form-control search adv-item" id="Description" autocomplete="off">
            </div>
            <div class="form-group">
                <label for="StartDate">@Model.Loc.Where(a => a.ResourceId == "globalStart").FirstOrDefault().Value.ToString() :</label>
                <input type="text" class="form-control search adv-item adv-dtpicker" id="StartDate" autocomplete="off">
            </div>
            <div class="form-group">
                <label for="EndDate">@Model.Loc.Where(a => a.ResourceId == "globalSpnEnd").FirstOrDefault().Value.ToString() :</label>
                <input type="text" class="form-control search adv-item adv-dtpicker" id="EndDate" autocomplete="off">
            </div>
        </div>
    </nav>
</form>

/*
 ******************************************************************************
 * PROPRIETARY DATA 
 ******************************************************************************
 * This work is PROPRIETARY to SOMAX Inc and is protected 
 * under Federal Law as an unpublished Copyrighted work and under State Law as 
 * a Trade Secret. 
 ******************************************************************************
 * Copyright (c) 2011 by SOMAX Inc.
 * All rights reserved. 
 ******************************************************************************
 * THIS CODE HAS BEEN GENERATED BY AN AUTOMATED PROCESS.
 * DO NOT MODIFY BY HAND.    MODIFY THE TEMPLATE AND REGENERATE THE CODE 
 * USING THE CURRENT DATABASE IF MODIFICATIONS ARE NEEDED.
 ******************************************************************************
 */

using System;
using System.Collections;
using System.Data;
using System.Data.SqlClient;
using Database.Business;
using System.Collections.Generic;
using Database.SqlClient;

namespace Database.StoredProcedure
{
    /// <summary>
    /// Access the usp_PermissionSet_RetrieveByClientId stored procedure.
    /// </summary>
    public class usp_PermissionSet_RetrieveByClientId
    {
        /// <summary>
        /// Constants.
        /// </summary>
        private static string STOREDPROCEDURE_NAME = "usp_PermissionSet_RetrieveByClientId";

        /// <summary>
        /// Default constructor.
        /// </summary>
        public usp_PermissionSet_RetrieveByClientId()
        {
        }

        /// <summary>
        /// Static method to call the usp_UserData_RetrieveByUserName stored procedure using SqlClient.
        /// </summary>
        /// <param name="command">SqlCommand object to use to call the stored procedure</param>
        /// <param name="processRow">ProcessRow delegate containing method to call to process the row into an object.</param>
        /// <param name="callerUserInfoId">long that identifies the user calling the database</param>
        /// <param name="callerUserName">string that identifies the user calling the database</param>
        /// <param name="clientId">long that contains the value of the @ClientId parameter</param>
        /// <returns>ArrayList containing the results of the query</returns>
        public static List<ArrayList> CallStoredProcedure(
            SqlCommand command,
            long callerUserInfoId,
            string callerUserName,
            b_PermissionSet obj
        )
        {
            List<ArrayList> records = new List<ArrayList>();
            SqlDataReader reader = null;
            SqlParameter RETURN_CODE_parameter = null;
            int retCode = 0;

            // Setup command.
            command.SetProcName(STOREDPROCEDURE_NAME);
            RETURN_CODE_parameter = command.GetReturnCodeParameter();
            command.SetInputParameter(SqlDbType.BigInt, "CallerUserInfoId", callerUserInfoId);
            command.SetStringInputParameter(SqlDbType.NVarChar, "CallerUserName", callerUserName, 256);
            command.SetInputParameter(SqlDbType.BigInt, "ClientId", obj.ClientId);


            try
            {
                // Execute stored procedure.
                reader = command.ExecuteReader();

                ArrayList tempRecords = new ArrayList();

                // Loop through dataset.
                while (reader.Read())
                {
                    // Add the record to the list.
                    Object site = b_Site.ProcessRow(reader);

                    tempRecords.Add(site);
                }

                records.Add(tempRecords);
                tempRecords = new ArrayList();

                reader.NextResult();

                // Loop through dataset.
                while (reader.Read())
                {
                    // Add the record to the list.
                    Object area = b_Area.ProcessRow(reader);

                    tempRecords.Add(area);
                }

                records.Add(tempRecords);
                tempRecords = new ArrayList();

                reader.NextResult();

                // Loop through dataset.
                while (reader.Read())
                {
                    // Add the record to the list.
                    Object department = b_Department.ProcessRow(reader);

                    tempRecords.Add(department);
                }

                records.Add(tempRecords);
                tempRecords = new ArrayList();

                reader.NextResult();

                // Loop through dataset.
                while (reader.Read())
                {
                    // Add the record to the list.
                    Object storeroom = b_Storeroom.ProcessRow(reader);

                    tempRecords.Add(storeroom);
                }

                records.Add(tempRecords);

            }
            finally
            {
                if (null != reader)
                {
                    if (false == reader.IsClosed)
                    {
                        reader.Close();
                    }
                    reader = null;
                }
            }

            // Process the RETURN_CODE parameter value
            retCode = (int)RETURN_CODE_parameter.Value;
            AbstractTransactionManager.CheckReturnCodeStatus(STOREDPROCEDURE_NAME, retCode);


            // Return the result
            return records;
        }
    }
}